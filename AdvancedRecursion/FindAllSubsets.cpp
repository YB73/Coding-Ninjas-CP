#include<bits/stdc++.h>
using namespace std;
vector<vector<int>> FindallSubses(int n, vector<int>&arr){
    vector<vector<int>> res;
    vector<int> temp;
    subset(arr,0,temp,ans);
    for(int i)
}
int main(){
    int n;
    cin>>n;
    vector<int> arr;
    for(int i=0; i<n; i++){
        int x;
        cin>>x;
        arr.push_back(x);
    }
    FindallSubsets(n, arr);

}